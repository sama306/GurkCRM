---
// Layout para páginas protegidas (/app/*). Provee QueryClientProvider + auth init.
import "../styles/global.css"
import { AppProvider } from "@/components/providers/AppProvider"
import { AuthGuard } from "@/components/auth/AuthGuard"
import { UserMenu } from "@/components/layout/UserMenu"
import { Toaster } from "@/components/ui/sonner"
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <slot name="head" />
  </head>
  <body>
    <!--
      AppProvider inicia TanStack Query y la verificación de sesión vía refresh token.
      AuthGuard (abajo) espera a que AppProvider termine y redirige a /login si no hay sesión.
    -->
    <AppProvider client:load />

    <header class="flex h-14 items-center justify-between border-b px-6">
      <a href="/app/dashboard" class="text-lg font-bold tracking-tight">
        GurkCRM
      </a>
      <UserMenu client:load />
    </header>

    <AuthGuard client:load>
      <main class="p-6">
        <slot />
      </main>
    </AuthGuard>

    <Toaster client:visible />
  </body>
</html>
