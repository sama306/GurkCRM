# Servidor
NODE_ENV=development
PORT=4000
CLIENT_URL=http://localhost:4321

# Base de datos (SQL Server)
DATABASE_URL="sqlserver://localhost:1433;database=gurkcrm;user=sa;password=sama0402;trustServerCertificate=true"

# JWT
JWT_ACCESS_SECRET=gurk-crm-access-secret-dev-2024
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_SECRET=gurk-crm-refresh-secret-dev-2024
JWT_REFRESH_EXPIRES_IN=7d

# Bcrypt
BCRYPT_SALT_ROUNDS=10

# Cookies
COOKIE_SECURE=false
COOKIE_DOMAIN=localhost

# Email (opcional en MVP — puede loguearse en consola en dev)
EMAIL_PROVIDER=console
EMAIL_FROM=no-reply@gurkcrm.com
